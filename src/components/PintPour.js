import React from 'react';
import PropTypes from 'prop-types';

function PintPour(props) {
    return (
        <React.Fragment>
            
        </React.Fragment>
    )

}

PintPour.propTypes = {
    handlePintPour: PropTypes.func,
    
}

export default PintPour;
